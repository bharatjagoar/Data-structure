#include<iostream>
using namespace std;
int main(){
    int arr [] = {1,2,3,4,5};
    int startingPointer=0;
    int lastPointer=(sizeof(arr)/sizeof(int))-1;
    while (lastPointer>-1&&startingPointer<(sizeof(arr)/sizeof(int)))
    {
        
    }
     
    return 0;
}